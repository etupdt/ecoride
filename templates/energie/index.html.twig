{% extends 'base.html.twig' %}

{% block title %}Voitures{% endblock %}

{% block body %}

    {{ form_errors(energieForm) }}

    <div class="container my-5">

        <h1 class="h3 mb-3 font-weight-normal">Marques</h1>

        {{ form_start(energieForm) }}

            <div class="form-group mx-5 my-3">
                {{ form_row(energieForm.libelle) }}
            </div>

            <div class="form-group mx-5 my-3">
                {{ form_row(energieForm.ecologique) }}
            </div>

            <div class="d-flex mx-5 mt-3 mb-5">
                <button type="submit" class="btn btn-primary">{{ action }}</button>
                <a href="{{ url('app_energies') }}" class="btn btn-primary mx-2">Abandonner</a>
            </div>

        {{ form_end(energieForm) }}

    </div>

{% endblock %}
