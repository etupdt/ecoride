{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<div class="container p-5">
    <divv class="container row">
        <div id="road" class="rounded-3 col-4"><img class="w-100" src={{asset('images/road.jpg')}} /></div>
        <divv class="container col">
            <div id="presentation" class="rounded-3 col-8 p-5 w-100">
                <p><b>EcoRide – Le covoiturage écologique et économique</b> 🌱🚗</p>
                <ul>
                    <li>Réduisez vos frais, partagez vos trajets, et agissez pour la planète.</li>
                    <li>Avec EcoRide, trouvez facilement un covoiturage en voiture, alliant <b>écologie, économie et simplicité.</b></li>
                </ul>
                <p>Notre ambition : devenir la référence du covoiturage pour les voyageurs soucieux de l’environnement.</p>
                </p>
            </div>
            {# <twig:Itineraire/> #}
            <form>
                <div class="input-group mb-3">

                    <span class="input-group-text">Choisissez un itinéraire</span>

                    <label class="input-group-text" for="_depart">Départ</label>
                    <input type="text" class="form-control"  list="list-ville" id="depart" name="depart" value="{{ depart }}"/>

                    <label class="input-group-text" for="_arrivee">Arrivée</label>
                    <input type="text" class="form-control"  list="list-ville" id="arrivee" name="arrivee" value="{{ arrivee }}"/>

                    <datalist id="list-ville">
                        {% for ville in villes %}      
                            <option data="{{ ville.id }}" value="{{ ville.libelle }}"></option>
                        {% endfor %}
                    </datalist>

                    <button class="btn btn-outline-primary" type="submit" id="itineraire-submit"><i class="bi bi-search"></i></button>

                </div>
            </form>
        </div>
    </div>
    <div class="container">
        <div id="car" class=""><img class="w-100" src="{{asset('images/small-car.jpg')}}"/></div>
    </div>
</div>
{% endblock %}
